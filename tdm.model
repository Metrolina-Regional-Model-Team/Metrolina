
/**
  The macro "tdm" holds the definition for the graphical flowchart of this model,
  with steps and links connecting steps.
**/
Macro "tdm" (Args)
//    Activity-Based Nested Logit Model
Body:
    // model = FlowChart( { StartNodes } , { EndNodes } , Descri
    model = FlowChart(null,null,null)
    Return(model)
endMacro
